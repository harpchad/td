# Plugin fixtures

Recorded HTTP responses for sync plugin development. Never live
credentials to a real Planner, monday.com, or Jira tenant.

If a response shape is missing, write the fixture by hand from the
vendor's published API documentation rather than pointing at a real
tenant to capture one.
